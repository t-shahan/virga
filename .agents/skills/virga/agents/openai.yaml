interface:
  display_name: "Virga"
  short_description: "Repo-specific patterns and workflows for virga"
  default_prompt: "Use the virga repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true